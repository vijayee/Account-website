var React= require('react');
var ReactDOM= require('react-dom');
var LoginForm = require('./Main');

// Render the main component into the dom
ReactDOM.render(<LoginForm />, document.getElementById('app'));
